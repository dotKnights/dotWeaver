# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "dotWeaver"

[setup]
script = '''
set -euo pipefail

cd "$CODEX_WORKTREE_PATH"

bun install --frozen-lockfile

cp "$HOME/.config/dotweaver/.env" .env

mkdir -p "$HOME/.dotweaver/workspaces"

bunx prisma generate
'''

[cleanup]
script = '''
set -euo pipefail

cd "$CODEX_WORKTREE_PATH"

rm -rf node_modules/.vite .svelte-kit build coverage test-results playwright-report
'''
